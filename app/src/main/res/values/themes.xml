<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Минимальная тема-обёртка для MainActivity. Нужна только до того, как
        Compose подключит свой SpellTrackerTheme: edge-to-edge + NoActionBar.
        Все цвета/шрифты/поверхности рисуются Compose-кодом.
    -->
    <style name="Theme.SpellTracker" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
